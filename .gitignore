# File; .gitignore

# Local secrets — live API key, proxy credentials, VNC password. Tracked
# template lives in .env.example. NEVER commit the real .env.
.env

.vscode
.python-version
__pycache__
.idea
config.conf
generated_media
har_and_cookies
/.claude/settings.local.json
